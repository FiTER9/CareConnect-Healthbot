# CareConnect 医疗健康助手 - 运行指南

本项目是一个基于 RAG (检索增强生成) 的医疗健康问答助手。经过修改，现在默认使用 **DeepSeek** (deepseek-r1:8b) 作为大语言模型，并使用 **text2vec-base-chinese** 作为嵌入模型，以提供更好的中文支持。

## 前置要求

1.  **Python 3.8+**: 请确保已安装 Python 环境。
2.  **Ollama**: 本项目依赖 Ollama 来运行本地大语言模型。请访问 [Ollama 官网](https://ollama.com/) 下载并安装。

## 快速开始

### 1. 安装依赖

在项目根目录下打开终端，运行以下命令安装 Python 依赖库：

```bash
pip install -r requirements.txt
```

### 2. 准备大语言模型 (DeepSeek)

安装完 Ollama 后，打开终端运行以下命令来下载 DeepSeek 模型：

```bash
ollama pull deepseek-r1:8b
```

> **注意**: 
> - 如果你想使用其他版本的 DeepSeek (如 `deepseek-r1:7b` 或 `deepseek-v2`)，请修改 `chainlit_app.py` 中的 `MODEL_NAME` 变量。
> - 请确保 Ollama 服务在后台运行 (`ollama serve`)。

### 3. 构建知识库

将你的 PDF 医疗文档放入 `data/` 目录中。然后运行以下命令来创建向量数据库：

```bash
python ingest.py
```

该脚本会：
1. 读取 `data/` 目录下的所有 PDF 文件。
2. 使用 `shibing624/text2vec-base-chinese` 模型将文本转化为向量。
3. 将向量索引保存到 `vectorstore/db_faiss` 目录。

> **提示**: 每次在 `data/` 目录添加新文件后，都需要重新运行此命令。

### 4. 启动应用

运行以下命令启动 Chainlit Web 界面：

```bash
chainlit run chainlit_app.py -w
```

启动后，浏览器会自动打开 `http://localhost:8000`。你可以开始与医疗助手对话了！

## 常见问题

**Q: 启动时报错 "Connection refused"？**
A: 请检查 Ollama 是否正在运行。你可以在终端输入 `ollama list` 来检查服务状态。

**Q: 回答不准确或找不到相关信息？**
A: 
1. 确保你的 PDF 文档内容清晰且已被放在 `data/` 目录。
2. 确保你运行了 `python ingest.py` 更新知识库。
3. RAG 系统的效果取决于检索到的上下文，尝试优化 PDF 文档的格式。

**Q: 显存不足？**
A: `deepseek-r1:8b` 模型需要约 5-6GB 显存 (或内存)。如果配置较低，可以尝试更小的模型 (如 `qwen:4b`)，并在 `chainlit_app.py` 中修改 `MODEL_NAME`。
